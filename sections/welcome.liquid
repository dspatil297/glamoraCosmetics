{% comment %} Glamora Welcome / Brand Introduction — centered text on cream {% endcomment %}
<div class="welcome-section shopify-section">
  <div class="welcome-section__inner">
    <span class="welcome-section__line" aria-hidden="true"></span>
    <h2 class="welcome-section__title">{{ section.settings.heading | default: 'Welcome to Glamora' }}</h2>
    <p class="welcome-section__tagline">{{ section.settings.tagline | default: '"Your Daily Dose of Glamour"' }}</p>
    <div class="welcome-section__body">
      {{ section.settings.body | default: 'At Glamora, we believe true beauty shines through healthy, glowing skin. With advanced formulas and premium ingredients, our anti-pollution skincare protects against environmental stressors while deeply hydrating and revitalizing. Blending nature and science, we deliver radiant, refreshed skin — every day.' }}
    </div>
  </div>
</div>

{% stylesheet %}
  .welcome-section { 
    background: var(--color-background); 
    padding: var(--section-padding-block-mobile) clamp(1rem, 4vw, var(--page-margin)); 
  }
  @media (min-width: 769px) {
    .welcome-section { padding: var(--section-padding-block-desktop) clamp(1rem, 4vw, var(--page-margin)); }
  }
  .welcome-section__inner { 
    max-width: 42rem; 
    margin: 0 auto; 
    text-align: center; 
  }
  .welcome-section__line { 
    display: block; 
    width: 3rem; 
    height: 3px; 
    background: var(--color-accent); 
    margin: 0 auto var(--spacing-md); 
  }
  .welcome-section__title { 
    font-family: var(--font-heading); 
    font-size: clamp(1.75rem, 3vw, 2.25rem); 
    margin: 0 0 var(--spacing-xs); 
    color: var(--color-foreground); 
  }
  .welcome-section__tagline { 
    font-size: 1rem; 
    color: var(--color-text-muted); 
    margin: 0 0 var(--spacing-lg); 
    font-style: italic; 
  }
  .welcome-section__body { 
    font-size: 1rem; 
    line-height: 1.7; 
    color: var(--color-foreground); 
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Welcome",
  "tag": "section",
  "class": "section-welcome",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Welcome to Glamora" },
    { "type": "text", "id": "tagline", "label": "Tagline", "default": "\"Your Daily Dose of Glamour\"" },
    { "type": "richtext", "id": "body", "label": "Body" }
  ],
  "presets": [{ "name": "Welcome" }]
}
{% endschema %}
