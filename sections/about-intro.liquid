{% comment %}
  About Intro Section - Hero section with company story
{% endcomment %}

<section class="about-intro">
  <div class="about-intro__container">
    {% if section.settings.hero_image %}
      <div class="about-intro__image">
        <img 
          src="{{ section.settings.hero_image | image_url: width: 900 }}" 
          alt="{{ section.settings.hero_image.alt | default: 'About Sarvital' }}"
          loading="eager"
          fetchpriority="high"
          decoding="async"
          width="1400"
          height="600"
        >
        <div class="about-intro__image-overlay"></div>
      </div>
    {% endif %}
    
    <div class="about-intro__content">
      {% if section.settings.heading != blank %}
        <h1 class="about-intro__heading">{{ section.settings.heading }}</h1>
      {% endif %}
      
      {% if section.settings.subheading != blank %}
        <p class="about-intro__subheading">{{ section.settings.subheading }}</p>
      {% endif %}
      
      {% if section.settings.company_story != blank %}
        <div class="about-intro__story">
          {{ section.settings.company_story }}
        </div>
      {% endif %}
      
      {% if section.settings.founders_message != blank %}
        <div class="about-intro__founders-message">
          <div class="founders-message">
            {% if section.settings.founders_name != blank %}
              <div class="founders-message__author">
                <span class="founders-message__name">{{ section.settings.founders_name }}</span>
                {% if section.settings.founders_title != blank %}
                  <span class="founders-message__title">{{ section.settings.founders_title }}</span>
                {% endif %}
              </div>
            {% endif %}
            <blockquote class="founders-message__quote">
              {{ section.settings.founders_message }}
            </blockquote>
          </div>
        </div>
      {% endif %}
    </div>
  </div>
</section>

{% stylesheet %}
  .about-intro {
    width: 100%;
    max-width: 100%;
    padding: var(--spacing-5xl) var(--spacing-lg);
    background: linear-gradient(180deg, var(--white) 0%, rgba(241, 241, 241, 0.7) 100%);
    box-sizing: border-box;
  }

  .about-intro__container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .about-intro__image {
    position: relative;
    width: 100%;
    height: 500px;
    border-radius: var(--radius-xl);
    overflow: hidden;
    margin-bottom: var(--spacing-4xl);
    box-shadow: 0 20px 60px rgba(45, 80, 22, 0.15);
  }

  .about-intro__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .about-intro__image-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.3) 0%, rgba(0, 0, 0, 0.2) 100%);
  }

  .about-intro__content {
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
  }

  .about-intro__heading {
    font-family: var(--font-heading);
    font-size: clamp(40px, 5vw, 64px);
    font-weight: 900;
    background: linear-gradient(135deg, var(--forest-green) 0%, var(--sage-green) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: var(--spacing-lg);
    letter-spacing: -0.02em;
    line-height: 1.2;
    padding: 10px;
    margin-top: var(--spacing-2xl);
  }

  .about-intro__subheading {
    font-size: clamp(20px, 2.5vw, 28px);
    font-weight: 600;
    color: var(--forest-green);
    margin-bottom: var(--spacing-xl);
    line-height: 1.4;
  }

  .about-intro__story {
    font-size: var(--body-large);
    line-height: 1.8;
    color: var(--text);
    margin-bottom: var(--spacing-4xl);
    text-align: left;
  }

  .about-intro__founders-message {
    margin-top: var(--spacing-4xl);
    padding-top: var(--spacing-4xl);
    border-top: 2px solid rgba(222, 222, 222, 0.8);
  }

  .founders-message {
    background: linear-gradient(135deg, rgba(21, 115, 109, 0.06) 0%, rgba(255, 255, 255, 0.04) 100%);
    padding: var(--spacing-2xl);
    border-radius: var(--radius-xl);
    border-left: 4px solid var(--sage-green);
    text-align: left;
  }

  .founders-message__quote {
    font-size: var(--body-large);
    font-style: italic;
    line-height: 1.8;
    color: var(--text);
    margin: 0;
    padding-left: var(--spacing-lg);
    position: relative;
  }

  .founders-message__quote::before {
    content: '"';
    position: absolute;
    left: 0;
    top: -10px;
    font-size: 60px;
    color: var(--sage-green);
    font-family: var(--font-heading);
    line-height: 1;
  }

  .founders-message__author {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
    margin-top: var(--spacing-lg);
    padding-top: var(--spacing-lg);
    border-top: 1px solid rgba(222, 222, 222, 0.8);
  }

  .founders-message__name {
    font-weight: 700;
    color: var(--forest-green);
    font-size: var(--body-normal);
  }

  .founders-message__title {
    font-size: var(--body-small);
    color: #707070;
  }

  @media (max-width: 768px) {
    .about-intro__image {
      height: 300px;
      margin-bottom: var(--spacing-2xl);
    }

    .about-intro__story {
      text-align: center;
    }

    .founders-message {
      padding: var(--spacing-lg);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "About Intro",
  "settings": [
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "About Glamora Cosmetics"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "At Glamora, we believe skincare is more than a routine\u2014it\u2019s a daily ritual of self-love. Our anti-pollution skincare is crafted to protect, hydrate, and enhance your natural glow in the modern urban world."
    },
    {
      "type": "richtext",
      "id": "company_story",
      "label": "Company Story",
      "default": "<p>Glamora began with a simple question: why does city life have to be so hard on our skin? Between pollution, UV exposure, long commutes, and late nights, we struggled to find products that truly protected and nourished without feeling heavy or complicated.</p><p>Instead of settling, we set out to create something better: city-smart skincare that feels like a ritual of self-love, delivers visible results, and fits seamlessly into a busy, modern life. Every Glamora formula is crafted to protect, hydrate, and enhance your natural glow&mdash;so your skin feels as confident as you do.</p>"
    },
    {
      "type": "richtext",
      "id": "founders_message",
      "label": "Founder's Message",
      "default": "<p>&ldquo;Our vision with Glamora is simple: skincare that keeps up with your city. We combine clinically-backed actives with skin-loving botanicals to defend against pollution and daily stress, while still feeling comforting, luxurious, and easy to use every single day.&rdquo;</p>"
    },
    {
      "type": "text",
      "id": "founders_name",
      "label": "Founder's Name",
      "default": "The Glamora Team"
    },
    {
      "type": "text",
      "id": "founders_title",
      "label": "Founder's Title",
      "default": "Founders, Glamora Skincare"
    }
  ],
  "presets": [
    {
      "name": "About Intro"
    }
  ]
}
{% endschema %}

