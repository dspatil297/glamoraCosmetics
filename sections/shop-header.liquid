<section class="shop-header">
  {% if section.settings.show_background_image and section.settings.background_image %}
    <div class="shop-header__background">
      <img 
        src="{{ section.settings.background_image | image_url: width: 1280 }}" 
        alt="{{ collection.title }}"
        loading="lazy"
        width="1920"
        height="400"
      >
      <div class="shop-header__overlay"></div>
    </div>
  {% endif %}
  
  <div class="shop-header__container">
    <div class="shop-header__content">
      <nav class="shop-header__breadcrumbs" aria-label="Breadcrumb">
        <a href="{{ routes.root_url }}" class="shop-header__breadcrumb-link">{{ section.settings.breadcrumb_home | default: 'Home' }}</a>
        <span class="shop-header__breadcrumb-separator">›</span>
        <span class="shop-header__breadcrumb-current">{{ collection.title }}</span>
      </nav>

      <h1 class="shop-header__title">{{ collection.title }}</h1>

      {% if collection.description != blank %}
        <p class="shop-header__subtitle">
          {{ collection.description | strip_html }}
        </p>
      {% endif %}

      <p class="shop-header__meta">
        {{ collection.products_count }} product{% if collection.products_count != 1 %}s{% endif %}
      </p>
    </div>
  </div>
</section>

{% stylesheet %}
  .shop-header {
    position: relative;
    width: 100%;
    max-width: 100%;
    padding: 60px var(--spacing-lg) 50px;
    background: #fafafa;
    border-bottom: 1px solid rgba(74, 124, 44, 0.1);
    overflow: hidden;
    box-sizing: border-box;
  }

  .shop-header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
      linear-gradient(135deg, rgba(74, 124, 44, 0.03) 0%, transparent 50%),
      linear-gradient(225deg, rgba(74, 124, 44, 0.04) 0%, transparent 50%);
    pointer-events: none;
  }

  .shop-header__background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .shop-header__background img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .shop-header__overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.95) 0%, rgba(255, 255, 255, 0.85) 100%);
  }

  .shop-header__container {
    position: relative;
    z-index: 2;
    max-width: 1400px;
    margin: 0 auto;
  }

  .shop-header__content {
    position: relative;
    text-align: left;
  }

  .shop-header__title {
    font-family: var(--font-heading);
    font-size: clamp(42px, 6vw, 64px);
    font-weight: 900;
    color: var(--forest-green);
    margin: 4px 0 8px 0;
    letter-spacing: -0.03em;
    line-height: 1.1;
    display: inline-block;
  }

  .shop-header__breadcrumbs {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 13px;
    color: rgba(76, 85, 100, 0.9);
    margin-bottom: 4px;
  }
  .shop-header__breadcrumb-link {
    color: inherit;
    text-decoration: none;
  }
  .shop-header__breadcrumb-link:hover {
    text-decoration: underline;
  }
  .shop-header__breadcrumb-current {
    color: rgba(76, 85, 100, 1);
  }

  .shop-header__subtitle {
    margin: 0 0 6px 0;
    max-width: 640px;
    font-size: 15px;
    color: rgba(76, 85, 100, 0.95);
    line-height: 1.7;
  }

  .shop-header__meta {
    margin: 0;
    font-size: 13px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: rgba(76, 85, 100, 0.9);
  }

  @media (max-width: 768px) {
    .shop-header {
      padding: 40px var(--spacing-md) 32px;
    }

    .shop-header__title {
      font-size: clamp(32px, 8vw, 48px);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Shop Header",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_background_image",
      "label": "Show Background Image",
      "default": false
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    },
    {
      "type": "text",
      "id": "breadcrumb_home",
      "label": "Breadcrumb Home label",
      "default": "Home"
    }
  ],
  "presets": [
    {
      "name": "Shop Header"
    }
  ]
}
{% endschema %}
