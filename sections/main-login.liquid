{% comment %}
  Main Login Section - Customer login with email/password, forgot password, register link.
  Google OAuth: Enable "Login with Google" in Shopify Admin → Settings → Customer accounts.
  Once enabled, Shopify may show the Google sign-in option on the login flow.
{% endcomment %}

<section class="login-section">
  <div class="login-section__container">
    {% if section.settings.title != blank %}
      <h1 class="login-section__title">{{ section.settings.title }}</h1>
    {% else %}
      <h1 class="login-section__title">{{ 'customers.login.title' | t | default: 'Login' }}</h1>
    {% endif %}

    {% if section.settings.subtitle != blank %}
      <p class="login-section__subtitle">{{ section.settings.subtitle }}</p>
    {% endif %}

    <div class="login-card">
      {% form 'customer_login', return_to: routes.root_url, class: 'login-form', id: 'customer-login' %}
        {{ form.errors | default_errors }}

        <div class="login-form__field">
          <label for="CustomerEmail" class="login-form__label">
            {{ 'customers.login.email' | t | default: 'Email' }}
          </label>
          <input
            type="email"
            id="CustomerEmail"
            name="customer[email]"
            class="login-form__input"
            autocomplete="email"
            required
            value="{% if form.email %}{{ form.email }}{% endif %}"
            placeholder="{{ 'customers.login.email' | t | default: 'Email' }}"
          >
        </div>

        <div class="login-form__field">
          <label for="CustomerPassword" class="login-form__label">
            {{ 'customers.login.password' | t | default: 'Password' }}
          </label>
          <input
            type="password"
            id="CustomerPassword"
            name="customer[password]"
            class="login-form__input"
            autocomplete="current-password"
            required
            placeholder="{{ 'customers.login.password' | t | default: 'Password' }}"
          >
        </div>

        <div class="login-form__row">
          {% if section.settings.show_remember %}
            <label class="login-form__checkbox-label">
              <input type="checkbox" name="customer[remember_me]" value="1" class="login-form__checkbox">
              <span>{{ 'customers.login.remember_me' | t | default: 'Remember me' }}</span>
            </label>
          {% endif %}
          <a href="{{ routes.account_recover_url }}" class="login-form__forgot">
            {{ 'customers.login.forgot_password' | t | default: 'Forgot your password?' }}
          </a>
        </div>

        <div class="login-form__submit">
          <button type="submit" class="btn btn-primary btn-large login-form__btn">
            {{ 'customers.login.submit' | t | default: 'Sign in' }}
          </button>
        </div>
      {% endform %}

      {% if shop.customer_accounts_optional and section.settings.show_register_link %}
        <p class="login-section__register">
          {{ 'customers.login.new_customer' | t | default: "Don't have an account?" }}
          <a href="{{ routes.account_register_url }}" class="login-section__register-link">
            {{ 'customers.login.create_account' | t | default: 'Create account' }}
          </a>
        </p>
      {% endif %}

      {% if section.settings.show_google_note %}
        <div class="login-section__divider">
          <span>{{ 'customers.login.or' | t | default: 'Or' }}</span>
        </div>
        <div class="login-section__google-note">
          <p>{{ 'customers.login.google_note' | t | default: 'Sign in with Google — enable in Shopify Admin: Settings → Customer accounts → Login with Google (connect your Google OAuth app there).' }}</p>
        </div>
      {% endif %}
    </div>

    {% if shop.checkout.guest_login and section.settings.show_guest %}
      <div class="login-section__guest">
        {% form 'guest_login', class: 'login-form login-form--guest' %}
          <button type="submit" class="btn btn-outline">
            {{ 'customers.login.continue_guest' | t | default: 'Continue as guest' }}
          </button>
        {% endform %}
      </div>
    {% endif %}
  </div>
</section>

{% stylesheet %}
  .login-section {
    width: 100%;
    max-width: 100%;
    padding: var(--spacing-2xl) var(--spacing-lg) var(--spacing-5xl);
    background: linear-gradient(180deg, var(--white) 0%, rgba(241, 241, 241, 0.7) 50%, var(--white) 100%);
    box-sizing: border-box;
    min-height: 60vh;
    display: flex;
    align-items: flex-start;
    justify-content: center;
  }

  .login-section__container {
    max-width: 460px;
    margin: 0 auto;
    width: 100%;
  }

  .login-section__title {
    font-family: var(--font-heading);
    font-size: clamp(28px, 4vw, 40px);
    font-weight: 900;
    color: var(--forest-green);
    text-align: center;
    margin: 0 0 var(--spacing-md);
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  .login-section__subtitle {
    text-align: center;
    font-size: var(--body-normal);
    color: var(--text);
    margin: 0 0 var(--spacing-xl);
    line-height: 1.6;
  }

  .login-card {
    background: var(--white);
    padding: var(--spacing-xl) var(--spacing-lg);
    border-radius: var(--radius-xl);
    box-shadow: 0 10px 40px rgba(45, 80, 22, 0.1);
    border: 2px solid rgba(222, 222, 222, 0.8);
  }

  .login-form {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }

  .login-form__field {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
  }

  .login-form__label {
    font-weight: 600;
    color: var(--forest-green);
    font-size: var(--body-small);
  }

  .login-form__input {
    padding: var(--spacing-md);
    border: 2px solid rgba(222, 222, 222, 0.8);
    border-radius: var(--radius-md);
    font-size: var(--body-normal);
    font-family: var(--font-body);
    color: var(--text);
    background: var(--white);
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
    width: 100%;
    box-sizing: border-box;
  }

  .login-form__input:focus {
    outline: none;
    border-color: var(--sage-green);
    box-shadow: 0 0 0 3px rgba(74, 124, 44, 0.1);
  }

  .login-form__row {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing-sm);
  }

  .login-form__checkbox-label {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-xs);
    font-size: var(--body-small);
    color: var(--text);
    cursor: pointer;
  }

  .login-form__checkbox {
    width: 18px;
    height: 18px;
    accent-color: var(--forest-green);
  }

  .login-form__forgot {
    font-size: var(--body-small);
    color: var(--sage-green);
    text-decoration: none;
    font-weight: 500;
  }

  .login-form__forgot:hover {
    text-decoration: underline;
    color: var(--forest-green);
  }

  .login-form__submit {
    margin-top: var(--spacing-sm);
  }

  .login-form__btn {
    width: 100%;
    justify-content: center;
  }

  .login-section__register {
    text-align: center;
    margin: var(--spacing-lg) 0 0;
    padding-top: var(--spacing-lg);
    border-top: 1px solid var(--border-color);
    font-size: var(--body-normal);
    color: var(--text);
  }

  .login-section__register-link {
    color: var(--sage-green);
    font-weight: 600;
    text-decoration: none;
    margin-left: var(--spacing-xs);
  }

  .login-section__register-link:hover {
    text-decoration: underline;
    color: var(--forest-green);
  }

  .login-section__divider {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    margin: var(--spacing-lg) 0 0;
    padding-top: var(--spacing-lg);
  }

  .login-section__divider::before,
  .login-section__divider::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--border-color);
  }

  .login-section__divider span {
    font-size: var(--body-small);
    color: var(--text);
    flex-shrink: 0;
  }

  .login-section__google-note {
    margin-top: var(--spacing-md);
    padding: var(--spacing-md);
    background: rgba(21, 115, 109, 0.06);
    border-radius: var(--radius-md);
    border: 1px solid rgba(21, 115, 109, 0.2);
  }

  .login-section__google-note p {
    margin: 0;
    font-size: var(--body-small);
    color: var(--text);
    line-height: 1.5;
  }

  .login-section__guest {
    margin-top: var(--spacing-xl);
    text-align: center;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Login",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Sign in to your Glamora account"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Access your orders, saved addresses, and personalized skincare recommendations in one place."
    },
    {
      "type": "checkbox",
      "id": "show_remember",
      "label": "Show \"Remember me\"",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_register_link",
      "label": "Show \"Create account\" link",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_google_note",
      "label": "Show Google sign-in note",
      "default": true,
      "info": "Reminds customers that Sign in with Google can be enabled in Shopify Admin."
    },
    {
      "type": "checkbox",
      "id": "show_guest",
      "label": "Show \"Continue as guest\" when from checkout",
      "default": true
    }
  ]
}
{% endschema %}
